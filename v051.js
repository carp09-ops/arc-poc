// Arc 0.5.1 — Tool Depth Pass: richer workout, nutrition, body check-in, signal detail.
(function(){
if(typeof S==='undefined')return;
const H=()=>S.history||{workouts:[],nutrition:[],body:[],activity:[],sleep:[],signals:[]};
const esc5=v=>esc(v==null?'':String(v));
const day5=()=>new Date().toISOString().slice(0,10);
const fmt5=d=>{try{return new Date(d+'T12:00:00').toLocaleDateString(undefined,{weekday:'short',month:'short',day:'numeric'})}catch(e){return d||''}};
const sigs5=()=>{try{return ArcData?.evaluateSignals?.()||H().signals||[]}catch(e){return H().signals||[]}};
const latest5=a=>a&&a.length?a.slice().sort((x,y)=>String(x.date).localeCompare(String(y.date))).at(-1):null;
const bodyMetrics=[['weight','Weight','lb'],['waist','Waist','in'],['hips','Hips','in'],['chest','Chest','in'],['thigh','Thigh','in'],['arm','Arm','in']];
function exList(name){try{return exerciseSet(name)||[]}catch(e){return [{name:'Goblet Squat',sets:3,reps:10},{name:'Romanian Deadlift',sets:3,reps:10},{name:'Supported Row',sets:3,reps:12},{name:'Floor Press',sets:3,reps:10},{name:'Dead Bug',sets:3,reps:8}]}}
function previousExercise(name){const rows=H().workouts.slice().reverse();for(const w of rows){const e=(w.exercises||[]).find(x=>(x.name||'')===name);if(e)return {date:w.date,ex:e}}return null}
function activeData(){const q=plan(),name=S.activeWorkout?.name||q.split?.[0]||'Full Body Strength';if(!S.activeWorkout)S.activeWorkout={name,index:0,sets:{}};const list=exList(name),idx=Math.min(S.activeWorkout.index||0,Math.max(0,list.length-1));return{name,list,idx,ex:list[idx]||{name:'Movement',sets:3,reps:10}}}
workout=function(){const a=activeData(),ex=a.ex,prev=previousExercise(ex.name),sets=S.activeWorkout.sets[ex.name]||(S.activeWorkout.sets[ex.name]=Array.from({length:+ex.sets||3},(_,i)=>({weight:'',reps:ex.reps||10,done:false})));const done=sets.filter(x=>x.done).length,pct=Math.round(((a.idx+(done/Math.max(1,sets.length)))/Math.max(1,a.list.length))*100);save();shell(`<div class="tool-workout"><header class="tool-top"><button data-go="train">←</button><div><small>WORKOUT IN PROGRESS</small><b>${esc5(a.name)}</b></div><span>${a.idx+1} / ${a.list.length}</span></header><div class="tool-progress"><i style="width:${pct}%"></i></div><section class="exercise-cinema"><div class="exercise-copy"><span class="eyebrow">Exercise ${a.idx+1} of ${a.list.length}</span><h1>${esc5(ex.name)}</h1><p>${ex.sets||3} sets · ${ex.reps||10} reps · controlled effort</p><div class="cue-chips"><span>Own the position</span><span>Control the tempo</span><span>Leave a clean rep</span></div></div><div class="exercise-status"><small>SESSION PROGRESS</small><b>${pct}%</b><span>${done} of ${sets.length} sets here</span></div></section><div class="tool-two"><section class="tool-panel"><div class="tool-panel-head"><div><span class="eyebrow">Working sets</span><h2>Log the work.</h2></div>${prev?`<div class="previous-pill"><small>LAST TIME</small><b>${fmt5(prev.date)}</b></div>`:''}</div><div class="set-table"><div class="set-table-head"><span>SET</span><span>LOAD</span><span>REPS</span><span>DONE</span></div>${sets.map((s,i)=>`<div class="deep-set ${s.done?'complete':''}"><b>${i+1}</b><input data-deep-weight="${i}" inputmode="decimal" placeholder="lb" value="${esc5(s.weight)}"><input data-deep-reps="${i}" inputmode="numeric" value="${esc5(s.reps)}"><button data-deep-done="${i}">${s.done?'✓':'○'}</button></div>`).join('')}</div>${prev?`<div class="previous-performance"><span>Previous performance</span><div>${(prev.ex.sets||[]).slice(0,3).map((x,i)=>`<b>${x.weight||'—'} lb × ${x.reps||'—'}</b>`).join('')}</div></div>`:''}</section><aside class="tool-panel coaching"><span class="eyebrow">Arc coaching</span><h2>Strong reps beat rushed reps.</h2><p>Use the last session as context, not a command. Progress can mean cleaner reps, more load, more reps, or simply repeating the work well.</p><div class="form-cues"><div><i>01</i><span><b>Set your position</b><small>Start each rep from control.</small></span></div><div><i>02</i><span><b>Keep the rep honest</b><small>Stop before technique changes.</small></span></div><div><i>03</i><span><b>Record what happened</b><small>Arc learns from completed work.</small></span></div></div></aside></div><div class="tool-sticky-action">${a.idx<a.list.length-1?`<button class="btn" data-deep-next ${done<sets.length?'disabled':''}>${done<sets.length?'Complete all sets to continue':'Next Exercise →'}</button>`:`<button class="btn" data-deep-finish ${done<sets.length?'disabled':''}>${done<sets.length?'Complete all sets to finish':'Complete Workout →'}</button>`}</div></div>`,null)};

nutrition=function(){const h=H(),today=h.nutrition.filter(x=>x.date===day5()),cal=today.reduce((a,x)=>a+(+x.calories||0),0),pro=today.reduce((a,x)=>a+(+x.protein||0),0),groups={};h.nutrition.forEach(x=>(groups[x.date]||(groups[x.date]=[])).push(x));const dates=Object.keys(groups).sort().reverse().slice(0,7),sig=sigs5().find(x=>(x.domains||[]).includes('Nutrition'));shell(`<div class="tool-page-head nutrition-tool-hero"><span class="eyebrow">Nutrition</span><h1>Your fuel journal.</h1><p>Capture enough context to understand patterns without turning every meal into a test.</p></div><div class="nutrition-depth"><div><section class="tool-panel today-fuel"><div class="fuel-big"><small>TODAY</small><b>${cal.toLocaleString()}</b><span>kcal logged</span></div><div class="fuel-big"><small>PROTEIN</small><b>${pro}g</b><span>logged today</span></div><div class="fuel-big"><small>MEALS</small><b>${today.length}</b><span>entries</span></div></section><section class="tool-panel"><div class="tool-panel-head"><div><span class="eyebrow">Quick log</span><h2>Add a meal.</h2></div><small>Useful, not perfect.</small></div><input id="food034" class="input" placeholder="Meal or food"><div class="deep-log-grid"><label><span>Calories</span><input id="cals034" class="input" type="number" inputmode="numeric" placeholder="0"></label><label><span>Protein</span><input id="prot034" class="input" type="number" inputmode="numeric" placeholder="0 g"></label></div><button class="btn" data-addmeal>Add to Today →</button></section><section class="tool-panel"><div class="tool-panel-head"><div><span class="eyebrow">Today</span><h2>Meal timeline</h2></div><span>${today.length} entries</span></div>${today.length?today.map((x,i)=>`<div class="meal-timeline"><i></i><div><small>${esc5(x.time||`Entry ${i+1}`)}</small><b>${esc5(x.food||x.name||'Meal')}</b><span>${x.calories||0} kcal · ${x.protein||0}g protein</span></div></div>`).join(''):'<div class="tool-empty">Nothing logged yet. Start with what actually happened.</div>'}</section></div><aside><section class="tool-panel intelligence-panel"><span class="eyebrow">Nutrition Intelligence</span><h2>${sig?esc5(sig.title):'Arc is building context.'}</h2><p>${sig?esc5(sig.summary):'Repeated nutrition and training days give Arc something meaningful to compare.'}</p>${sig?`<button class="v05-text-btn" data-signal-id="${esc5(sig.id)}">Explore evidence →</button>`:''}</section><section class="tool-panel"><div class="tool-panel-head"><div><span class="eyebrow">History</span><h2>Last 7 days</h2></div></div>${dates.length?dates.map(d=>{const m=groups[d],c=m.reduce((a,x)=>a+(+x.calories||0),0),p=m.reduce((a,x)=>a+(+x.protein||0),0);return `<div class="nutrition-day"><div><b>${fmt5(d)}</b><small>${m.length} meal${m.length===1?'':'s'}</small></div><span>${c.toLocaleString()} kcal</span><strong>${p}g protein</strong></div>`}).join(''):'<div class="tool-empty">History will appear as you log.</div>'}</section></aside></div>`, 'nutrition')};

function bodyCheckin(){const b=latest5(H().body)||S.profile||{};shell(`<div class="checkin-shell"><header class="tool-top"><button data-go="body">←</button><div><small>BODY CHECK-IN</small><b>A moment in the trend.</b></div><span>${fmt5(day5())}</span></header><section class="checkin-hero"><span class="eyebrow">Private by default</span><h1>Record where you are today.</h1><p>No score. No judgment. These measurements become useful only when Arc can compare them over time.</p></section><div class="checkin-layout"><section class="tool-panel"><div class="tool-panel-head"><div><span class="eyebrow">Measurements</span><h2>Today’s check-in</h2></div><small>Leave anything blank</small></div><div class="checkin-grid">${bodyMetrics.map(([k,l,u])=>`<label><span>${l}</span><div><input id="body-${k}" class="input" type="number" step="0.1" inputmode="decimal" placeholder="—" value="${esc5(b[k]||'')}"><b>${u}</b></div></label>`).join('')}</div><button class="btn" data-save-body>Save Check-in →</button></section><aside class="tool-panel checkin-context"><span class="eyebrow">Why Arc asks</span><h2>One number is noisy. A pattern is useful.</h2><p>Arc can compare body changes with training, nutrition and activity only after repeated check-ins create a real timeline.</p><div class="form-cues"><div><i>01</i><span><b>Use similar conditions</b><small>Consistency improves comparability.</small></span></div><div><i>02</i><span><b>Don’t chase daily noise</b><small>Weekly is enough for the POC.</small></span></div><div><i>03</i><span><b>Let the trend speak</b><small>No single value defines progress.</small></span></div></div></aside></div></div>`,null)}

function signalDeep(id){const s=sigs5().find(x=>String(x.id)===String(id));if(!s)return;S.activeSignal=id;const e=s.evidence||{},entries=Object.entries(e).filter(([,v])=>typeof v!=='object');shell(`<div class="signal-report"><header class="tool-top"><button data-go="arc">←</button><div><small>BODY INTELLIGENCE REPORT</small><b>Arc Signal</b></div><span>${esc5(s.confidence||'Learning')}</span></header><section class="signal-report-hero"><div><span class="eyebrow">${s.status==='active'?'Earned Signal':'Arc is watching'}</span><h1>${esc5(s.title)}</h1><p>${esc5(s.summary)}</p></div><div class="signal-confidence"><small>CONFIDENCE</small><b>${esc5(s.confidence||'Learning')}</b><span>${s.windowDays||'—'} day window</span></div></section><div class="signal-report-grid"><section class="tool-panel"><span class="eyebrow">What Arc noticed</span><h2>The observation.</h2><p>${esc5(s.summary)}</p><div class="signal-domains">${(s.domains||[]).map(x=>`<span>${esc5(x)}</span>`).join('')}</div></section><section class="tool-panel"><span class="eyebrow">Supporting evidence</span><h2>What sits underneath it.</h2>${entries.length?entries.map(([k,v])=>`<div class="evidence-line"><span>${esc5(k.replace(/([A-Z])/g,' $1').replace(/^./,c=>c.toUpperCase()))}</span><b>${esc5(v)}</b></div>`).join(''):`<div class="tool-empty">Arc has enough repeated context to watch this pattern, but the rule does not expose additional metrics yet.</div>`}</section><section class="tool-panel interpretation"><span class="eyebrow">How to read this</span><h2>Evidence, not certainty.</h2><p>This Signal describes an observed pattern in your logged data. It does not prove that one behavior caused another outcome.</p><div class="interpret-rule"><i>✦</i><span><b>Use it as a question worth testing.</b><small>Keep behaving naturally and let more evidence strengthen, weaken, or change the pattern.</small></span></div></section></div><button class="btn secondary" data-go="arc">Back to Your Arc</button></div>`,null)}

document.addEventListener('click',e=>{const m=e.target.closest('[data-measure]');if(m){e.preventDefault();e.stopImmediatePropagation();bodyCheckin();return}const s=e.target.closest('[data-signal-id]');if(s){e.preventDefault();e.stopImmediatePropagation();signalDeep(s.dataset.signalId);return}const d=e.target.closest('[data-deep-done]');if(d){e.preventDefault();e.stopImmediatePropagation();const a=activeData(),sets=S.activeWorkout.sets[a.ex.name];sets[+d.dataset.deepDone].done=!sets[+d.dataset.deepDone].done;save();workout();return}const n=e.target.closest('[data-deep-next]');if(n&&!n.disabled){e.preventDefault();e.stopImmediatePropagation();S.activeWorkout.index=(S.activeWorkout.index||0)+1;save();workout();return}const f=e.target.closest('[data-deep-finish]');if(f&&!f.disabled){e.preventDefault();e.stopImmediatePropagation();const a=activeData(),stored=a.list.map(ex=>({name:ex.name,sets:S.activeWorkout.sets[ex.name]||[]}));const volume=stored.reduce((z,x)=>z+x.sets.reduce((q,y)=>q+(+y.weight||0)*(+y.reps||0),0),0);H().workouts.push({date:day5(),time:new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}),name:a.name,durationMin:S.profile.duration||30,exercises:stored,volume});S.logs.workouts=H().workouts.length;delete S.activeWorkout;S.screen='train';save();render();return}},true);
document.addEventListener('input',e=>{const w=e.target.closest('[data-deep-weight]'),r=e.target.closest('[data-deep-reps]');if(!w&&!r)return;const a=activeData(),sets=S.activeWorkout.sets[a.ex.name],i=+(w?w.dataset.deepWeight:r.dataset.deepReps);if(w)sets[i].weight=w.value;if(r)sets[i].reps=r.value;save()});
if(['workout','nutrition'].includes(S.screen))render();
})();